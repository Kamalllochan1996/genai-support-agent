services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: genai-support-agent-api

    ports:
      - "8001:8000"

    env_file:
      - .env

    restart: unless-stopped